Area | What to change | How to implement (React + Supabase stack)
1 · Date‑range selector | • Global picker at top‑right of dashboard.• Quick‑select chips: 7 d • 30 d • 90 d • 6 m • 1 y • 3 y • Custom calendar. | tsx<br><DateRangePicker<›  value={range} onChange={setRange} presets={[7,30,90,180,365,1095]} />Use react-date-range or shadcn/ui popover; store selection in URL query (e.g. ?from=&to=) and in Zustand context so every chart / query hook automatically re‑fetches with supabase.rpc('time_window', { from, to }).
2 · Pie / doughnut views | • Add a “Time Allocation” section beneath KPI cards.• Two pies: – Reality (actual minutes/group) – Goals (goal minutes/group)• Clicking a slice filters the list/table below. | Use react-chartjs-2 <Doughnut />; feed data from the same aggregate query that drives the progress bars.
3 · Category cards | • Replace the current plain list with cards: Category Name X h / Y h (actual/goal) colored progress bar (green ≥ goal, amber < goal).• Sub‑categories displayed in an expandable <Accordion> right under each card, inheriting the date‑range filter. | Reusable <CategoryCard category={c} /> component.
4 · Validation – sub‑goal ≤ category goal | Database‑first (safer):sql<br>CREATE OR REPLACE FUNCTION check_sub_goal() RETURNS TRIGGER AS $$<br>BEGIN<br>  IF (SELECT COALESCE(SUM(goal_minutes),0) FROM sub_categories WHERE category_id = NEW.category_id AND id <> NEW.id) + NEW.goal_minutes > (SELECT goal_minutes FROM categories WHERE id = NEW.category_id) THEN<br>    RAISE EXCEPTION 'Sub‑category goals exceed parent goal.';<br>  END IF;<br>  RETURN NEW;<br>END; $$ LANGUAGE plpgsql;<br>CREATE TRIGGER sub_goal_check BEFORE INSERT OR UPDATE ON sub_categories<br>  FOR EACH ROW EXECUTE FUNCTION check_sub_goal();Also mirror in the Settings UI: disable Save and show inline error if total exceeds limit. | 
5 · Unaccounted‑time metric | • During daily aggregation: unaccounted = 24 h – SUM(actual).• Display as a red “⚠ Unaccounted” badge on each day row; include a mini‑stacked bar (accounted vs unaccounted). | Extend daily_summary view: SELECT date, accounted_minutes, 1440‑accounted_minutes AS unaccounted….
6 · Single‑screen daily form (less friction) | • Merge “Time entry” and “True/False habits” into one scrollable form: a. Category accordion → sub‑category inputs (15‑min step). b. Boolean habit checklist. c. Sticky Save bar at bottom that shows unaccounted live (24 h – entered). | Build the page as /daily-entry and link it from the dashboard’s top bar (“+ Log today”). Use React Hook Form with a useFieldArray so new sub‑cats appear immediately.
7 · Dashboard cleanup | • Remove “Add Category” button from dashboard; leave it exclusively under Settings → Categories.Update route guards so only admins see the settings link if needed. | 